import java.sql.SQLOutput;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main2 { //Derleme Hatası Çünkü Manin2 isminde değil Main isminde olmalı. Main2 seçip refactor kısmından değiştir dersek class adı değişir
    public static void main(String[] args) { // MEsela main yerine Main yazarsak hata alırız bu keyword değiştirilemez. ilk harf büyükse classtır gibi gibi algılıyor
        Scanner scanner = new Scanner(System.in; // Parantez hatası, parantezlerin başlangıcı ve bitişi olmalı. Derleme hatasıdır
        Scanner scanner = new Scanner(System.in) // Noktalı virgül konulmalı. Derleme hatasıdır. Ekstra olarak zaten tanımlı hatası veriyor üstte olduğu için
        number1=5; // Bu şimdi tanımlı değil sadece değişken olursa tanımsız. veriTipi değskenAdi

        int number2;
        int number2=10; // İki kere tanımlama yapılmaz. Aynı değişken adı ile yan bir değişken oluşturmaya çalışıyoruz hatalıdır.

        int number3;
        if(number3>0); // Bunu yazmak istiyorsun ama bir değeri yok run yapınca otomatik 0'a eşitler

        boolean tf;
        System.out.println(tf);// aynı şekilde değeri yok


        String number5="5";
        int number4 = number5; // Veri tipi uyuşmazlığı var 5'i 4'e atamaz.

        Int number10; // direkt Scanner gibi kırmızıya çevirdi class olarak alıyor

        x(); //bu bir fonksiyondur aynı nextInt(); gibi

        String name="BURAK";
        name.toLowerCase(); //Bu bir fonksiyondur yazılanı küçük harflere çevirir

    }
}
